# eegvis backend dependencies. Keep in sync with pyproject.toml.
# Install with: pip install -r requirements.txt
numpy>=1.24
scipy>=1.10
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pyyaml>=6.0
typer>=0.9
rich>=13.0
websockets>=12.0

# Optional: real LSL hardware support (requires liblsl on the system).
# pip install pylsl
pylsl>=1.16

# Dev/test
pytest>=8.0
httpx>=0.27
